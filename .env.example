PORT=3000

# --- PostgreSQL (Neon) ---
# Full connection string, e.g.:
# postgres://USER:PASSWORD@YOUR_PROJECT.neon.tech:5432/DBNAME?sslmode=require
DATABASE_URL=

# (If DATABASE_URL is set the PG* vars below are ignored.)
PGHOST=
PGPORT=
PGUSER=
PGPASSWORD=
PGDATABASE=

# --- JWT ---
JWT_SECRET=changeme
